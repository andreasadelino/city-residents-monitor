version: '2'
services:
  cityresidencestrack-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/CityResidencesTrack/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=CityResidencesTrack
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
